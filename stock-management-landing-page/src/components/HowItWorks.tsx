export function HowItWorks() {
  return (
    <section className="how-it-works" id="como-funciona">
      <h2>¿CÓMO FUNCIONA?</h2>
      <ol>
        <li>La app de <strong>Fischer Stock Management</strong> simplifica muchísimo el recorrido de relevamiento, ahorrando tiempo de trabajo para los empleados y por lo tanto plata para tu empresa.</li>
        <li>No solo ahorras todo eso, sino que también podes monitorear desde tu celular un ranking de productos filtrado como vos quieras y una lista de empleados con cada visita con fecha y lugar.</li>
        <li>Siendo administrador en nuestra aplicación podes monitorear, crear, modificar y eliminar tanto productos como usuarios solamente con unos clicks.</li>
      </ol>
    </section>
  );
}

/*{steps.map((step, index) => (
  <li key={index}>{step}</li>
))} */